/**
 * {description}
 *
 * @author		{author}
 * @version		{version}
 * @date		{date}
 *
 * @see			{url}
 * @icon		{icon}
 */
(function()
{
	
	// --------------------------------------------------------------------------------
	// functions
	
		

	// --------------------------------------------------------------------------------
	// variables
	
		var name = '{name}';

	// --------------------------------------------------------------------------------
	// code
	
		xjsfl.init(this);
		if(UI.selection)
		{
			trace('Snippet "' +name+ '" ran!');
		}
	
})()